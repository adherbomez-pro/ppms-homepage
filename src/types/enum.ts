export enum RequestStatus {
    PENDING_APPROVAL = 'Pending approval',
    APPROVED = 'Approved',
    REJECTED = 'Rejected',
    PENDING = 'Pending',
}

export enum SectionType {
    PROFILE = 'profile',
    TRAINING = 'training',
    PROJECT = 'project',
    ORDER = 'order',
    DOCUMENT = 'document',
    INCIDENT = 'incident',
    QUOTE = 'quotes',
    DEFAULT = 'default',
}
